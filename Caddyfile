# ─────────────────────────────────────────────────────────────
# twitter-bot (凛) Caddyfile — テンプレート / ドキュメント用
#
# このファイルは直接使われない。デプロイスクリプトが
# Caddyfile.gen (gitignored) を生成して使用する。
# BCrypt ハッシュを .env 経由で渡すと $ が展開されて壊れるため、
# ハッシュはデプロイ時に Caddyfile.gen へ直接埋め込む方式。
#
# 【将来 TLS 対応】ドメイン取得後は deploy.yml の http://:80 を
# your-domain.com に差し替えると Let's Encrypt TLS が自動で有効になる。
# ─────────────────────────────────────────────────────────────

# http://:80 {
#     log { output stderr; format console }
#     encode gzip
#     header {
#         X-Content-Type-Options nosniff
#         X-Frame-Options DENY
#         Referrer-Policy strict-origin-when-cross-origin
#         Strict-Transport-Security "max-age=31536000; includeSubDomains"  # TLS時のみ有効化
#         -Server
#     }
#     basic_auth {
#         <DASHBOARD_BASIC_USER> <DASHBOARD_BASIC_PASS_HASH (BCrypt)>
#     }
#     reverse_proxy dashboard:3001 {
#         transport http { response_header_timeout 120s }
#     }
# }
